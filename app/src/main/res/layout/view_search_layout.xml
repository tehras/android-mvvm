<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
        android:background="@drawable/bg_rounded"
        android:padding="@dimen/default_padding"
        android:focusable="true"
        android:clickable="true"
        android:layout_height="wrap_content">

    <EditText
            android:layout_width="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:background="@null"
            android:hint="@string/search_layout_text_hint"
            android:layout_marginEnd="@dimen/default_padding"
            app:layout_constraintEnd_toStartOf="@+id/search_layout_clear"
            app:layout_constraintStart_toStartOf="parent"
            android:id="@+id/search_layout_input_text"
            android:layout_height="wrap_content"
            android:imeOptions="actionDone"
            android:inputType="text"
            android:importantForAutofill="no"
            tools:ignore="UnusedAttribute"/>

    <ImageView
            android:id="@+id/search_layout_clear"
            android:src="@drawable/ic_exit"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/clear_icon"/>

</androidx.constraintlayout.widget.ConstraintLayout>